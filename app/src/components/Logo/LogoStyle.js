import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  logoContainer: {
    alignItems: 'center',
    marginBottom: 10,
  },
  logo: {
    width: 140,
    height: 140,
  },
});

export default styles;
