import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  descriptionText: {
    textAlign: 'center',
    color: '#888888',
    fontSize: 12,
    lineHeight: 22,
    maxWidth: 320,
    fontWeight: '400',
  },
});

export default styles;
